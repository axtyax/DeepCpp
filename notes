To set the size of a signal, it could be done by defining the max dimensions and using the mapping to fill the value vector
    -NEXT TASK!

Make trivial mapping the default for a signal

generalized processing between layers (in Layer class)

TODO:
    -Error class
    -Gradient computation
    -Training class
    -Test training and confirm results